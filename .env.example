# Canonical domain used for metadataBase, canonical tags, sitemap, and OG URLs.
NEXT_PUBLIC_SITE_URL=https://unmatch.vercel.app

# Keep unset (or "false") until the real domain + final copy are live.
# Set to "true" in Vercel to allow search engine indexing.
NEXT_PUBLIC_ALLOW_INDEXING=false
